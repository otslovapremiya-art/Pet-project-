# STATUS.md — где мы сейчас

> Читается первым в начале каждой сессии. Обновляется последним в конце каждой.

**Дата последнего обновления:** 09.08.2026
**Текущий этап:** шаги 0 и 1 закрыты, следующий — шаг 2 (спайк «обрыв контекста»)

---

## Чем закончили прошлую сессию

**Шаг 0 — сетап — пройден полностью.** git установлен, заведён публичный репозиторий https://github.com/otslovapremiya-art/Pet-project- , первый коммит ушёл в `origin/main`. Откат проверен руками: файл испорчен и возвращён, затем удалён и возвращён.

**Шаг 1 — спайк «приёмка вслепую» — пройден.** Заведена папка `tasks/`, задача `tasks/задача-01.md` с восемью проверками, записанными до кода. Написаны `src/index.html`, `src/game.js`, `src/style.css`: число и кнопка, нажатие увеличивает число на единицу. Все восемь проверок пройдены руками, код при приёмке не открывался. **Вопрос №1 закрыт.**

Незакрытый хвост: список проверок писал Claude, Vitalii принял без правок. Допущение «владелец сформулирует проверки своими словами» осталось непроверенным и перенесено на задачу 02 — там проверки диктует Vitalii, Claude только записывает.

Рабочие замечания: команды выполняются в терминале VS Code (Terminal → New Terminal), это тот же PowerShell. Задачи живут файлами в `tasks/`, а не в GitHub Issues — шаблон поправлен.

Ключевые решения, принятые ранее и определяющие всё остальное:

- Игра — полигон, главный результат — отлаженная схема ведения проектов с Claude.
- Критерий успеха: Vitalii самостоятельно, без подсказок, разворачивает по этой схеме второй, другой проект.
- Стек: HTML + CSS + JavaScript, три файла, без сборки и сервера.
- Папка проекта подключена к сессии: файлы Claude правит на диске напрямую, команды в терминале Vitalii выполняет сам по готовым строкам.

Комплект документов создан: `PROJECT.md`, `CLAUDE.md`, `ARCHITECTURE.md`, `STATUS.md`, `CHANGELOG.md`, `TASK-TEMPLATE.md`, `.gitignore`.

## Что делаем следующим шагом

**Шаг 2 — спайк по вопросу №2** («обрыв контекста»). Закрыть эту сессию, начать новую с нуля и сказать «продолжай», не пересказывая ничего руками.

Пройдено, если новая сессия сама, без наводящих вопросов, назвала следующий шаг — задачу 02 — и не переспрашивала про прошлое.

## Что дальше, чтобы не забыть

**Задача 02 — автоматический прирост:** число растёт само, раз в секунду, без нажатий. Два условия к этой задаче:

1. Проверки диктует Vitalii своими словами — это долг с задачи 01.
2. Заполнить строку замера по вопросу №3 (таблица ниже) сразу после задачи, а не по памяти потом.

К остальным механикам игры (шаги 3–6 плана) переходим после задачи 02.

## Открытые вопросы

| # | Вопрос | Статус |
|---|---|---|
| 1 | Как проверить работу кода, не читая код | **закрыт 09.08.2026** — задача 01 принята по списку, код не открывался. Хвост: проверки писал Claude, долг на задачу 02 |
| 2 | Хватит ли `STATUS.md` для передачи контекста | открыт — закрывается шагом 2, следующей сессией |
| 3 | Не тяжелее ли ритуал самого проекта | открыт — замеряется по ходу первых трёх функций |

## Замер по вопросу №3

Время вписывает Vitalii по факту, сразу после задачи.

| Функция | Время на код | Время на документы и задачи |
|---|---|---|
| 1. Кнопка и счётчик | 3 минуты | около 3 часов |

Сессия целиком заняла около 5 часов. Из них разовый сетап (установка git, регистрация на GitHub, разбор ошибки соединения) — 30 минут, ещё примерно полтора часа съели перебои интернета. В замер идут только две колонки таблицы: сетап бывает один раз, связь к схеме отношения не имеет.

Соотношение 3 минуты против 3 часов выглядит приговором схеме, но выводов пока не делаем — правило из `PROJECT.md` требует три функции, а первая включала обучение с нуля: что такое терминал, коммит, откат. Смотрим, как изменится цифра на задачах 02 и 03.

Если бумага стабильно съедает больше половины — упрощаем схему и записываем это решение в `PROJECT.md`, часть 6.
